---
phase: 02.1-daily-task-report-inserted-urgent
plan: 03
type: execute
wave: 2
depends_on: ["02.1-02"]
files_modified:
  - backend/src/services/report.rs
autonomous: true
requirements: [RPT-02, RPT-04]
user_setup: []

must_haves:
  truths:
    - "The 'Missed yesterday' section lists tasks the system actually penalized yesterday, read from missed_task_penalties rather than recomputed (D-08)"
    - "Inverted (bad) habits never appear via the penalty half, because the background job rewards rather than penalizes them (D-09)"
    - "Inverted habits that WERE performed yesterday appear in the same section (D-10)"
    - "Both sources appear in one single unlabelled section (D-11)"
    - "Only the caller's own or unassigned tasks appear — missed_task_penalties has no user_id, so the filter is enforced by joining to tasks (D-13, D-15)"
    - "Archived, paused, pending-suggestion, one-time tasks and household vacation days are excluded (D-14)"
    - "Missed lines use the same shared formatter as due-today, including the due_time suffix (D-21)"
    - "An empty section renders exactly 'All tasks completed yesterday' (D-23)"
  artifacts:
    - path: "backend/src/services/report.rs"
      provides: "build_missed_yesterday_section implementation replacing the plan-02 stub, plus its tests"
      contains: "missed_task_penalties"
      min_lines: 450
  key_links:
    - from: "backend/src/services/report.rs::build_missed_yesterday_section"
      to: "missed_task_penalties table"
      via: "SELECT task_id WHERE due_date = yesterday (D-08 — materialized state, not recomputation)"
      pattern: "missed_task_penalties"
    - from: "backend/src/services/report.rs::build_missed_yesterday_section"
      to: "tasks table"
      via: "per-task join/lookup applying D-13 ownership and D-14 exclusions — missed_task_penalties has no user_id (D-15)"
      pattern: "assigned_user_id"
    - from: "backend/src/services/report.rs::build_missed_yesterday_section"
      to: "backend/src/services/household_settings.rs::is_household_on_vacation"
      via: "D-14 vacation exclusion, applied to the WHOLE section"
      pattern: "is_household_on_vacation"
    - from: "backend/src/services/report.rs::build_missed_yesterday_section"
      to: "backend/src/services/report.rs::format_report_line"
      via: "the one shared line formatter (D-21, DRY)"
      pattern: "format_report_line"
---

<objective>
Replace plan 02's `build_missed_yesterday_section` stub with the real implementation: everything
that went wrong yesterday, in one list.

Purpose: RPT-02. The user's framing is "Alles was gestern schief lief soll da drin stehen" — which
means two different data sources merged into one unlabelled section (D-11): good habits the system
actually penalized (D-08, read from `missed_task_penalties`), and bad habits that were indulged
(D-10, read from `task_completions`).

Output: a complete, tested `build_missed_yesterday_section` in `backend/src/services/report.rs`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-daily-task-report-inserted-urgent/02.1-CONTEXT.md
@.planning/phases/02.1-daily-task-report-inserted-urgent/02.1-RESEARCH.md
@.planning/phases/02.1-daily-task-report-inserted-urgent/02.1-VALIDATION.md
@.planning/phases/02.1-daily-task-report-inserted-urgent/02.1-01-SUMMARY.md
@.planning/phases/02.1-daily-task-report-inserted-urgent/02.1-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md

<toolchain>
Every command needs `nix develop -c`. Commit with `jj commit -m "..."`.

**Do NOT run `cargo sqlx prepare`** — there are zero SQLx compile-time macros in this backend and
no `sqlx-data.json`/`.sqlx/` exists. Use the runtime `sqlx::query_as::<_, T>(...)` /
`sqlx::query_scalar::<_, T>(...)` forms like every other query in the codebase.

**Do NOT add a `user_id` column or a migration to `missed_task_penalties`** — D-15 forbids it in
this phase. There is no schema change anywhere in Phase 2.1.
</toolchain>

<resolved_ambiguities>
DECIDED by the orchestrator — do not re-open:

1. **The D-14 vacation exclusion applies to the WHOLE "missed yesterday" section**, including the
   D-10 inverted-habit-performed query — not just the `missed_task_penalties` half. D-14 scopes it
   to the *section*, not to one data source. This matters because the two sources behave
   differently: `process_missed_tasks` already skips writing penalty rows during vacation, so the
   D-08 half is naturally vacation-clean, whereas `complete_task` never checks vacation mode, so
   the D-10 half is not. Apply an explicit guard.
2. **Sort order: by `due_time` ascending (`None` last), then title case-insensitively** — reuse
   plan 02's `sort_report_lines`, applied to the MERGED list.
3. **The D-21 `due_time` suffix applies here too**, via plan 02's single `format_report_line`.
   Build `ReportLine` values here, not bare `String`s.

**Vacation check date:** use `yesterday`, i.e. `is_household_on_vacation(&settings, yesterday)`.
Note this deliberately differs from `background_jobs::process_missed_tasks`, which checks
`today_local` because it runs at processing time. The report is describing *yesterday*, so
yesterday is the semantically correct day. Add a `//` comment recording this divergence.
</resolved_ambiguities>

<known_limitation>
D-10's wording names `task_completions.due_date = yesterday` explicitly. For `RecurrenceType::Weekdays`
and `RecurrenceType::Custom`, `scheduler::get_next_due_date` deliberately skips today when today is
itself a scheduled occurrence, so a bad habit indulged on those recurrence types can be stored with
a `due_date` that is not the calendar day of the act. Implement D-10 exactly as worded anyway:
"fixing" only this one query would create an internal inconsistency with D-08, which trusts the
background job's same-flavoured bookkeeping. Record it as a `// KNOWN LIMITATION:` comment next to
the query. Do NOT change `scheduler.rs`.
</known_limitation>

<known_out_of_scope_failure>
`frontend/src/components/solo_mode_banner.rs:66` trips `clippy::type_complexity`. PRE-EXISTING and
UNRELATED. Do not fix it, do not treat it as a regression.
</known_out_of_scope_failure>

<interfaces>
<!-- Contracts the executor needs. Do not go exploring. -->

**Contract fixed by plan 02 — implement against it, do not change it:**
`async fn build_missed_yesterday_section(pool: &SqlitePool, household_id: &Uuid, user_id: &Uuid, settings: &HouseholdSettings, yesterday: NaiveDate) -> Result<Vec<ReportLine>, ReportError>`

Available from plan 02 in the same file: `ReportLine { title: String, due_time: Option<String>, done: bool }`,
`format_report_line`, `sort_report_lines`, `ReportError`, `MISSED_YESTERDAY_EMPTY`,
`MISSED_YESTERDAY_HEADER`.

**`missed_task_penalties` schema** (`backend/migrations/20240125000000_missed_task_tracking.sql`,
mirrored into `test_utils` by plan 01): `task_id TEXT NOT NULL`, `due_date DATE NOT NULL`,
`processed_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP`, `PRIMARY KEY (task_id, due_date)`.
**There is no `user_id` column** (D-15).

**Fixture from plan 01:** `test_utils::insert_missed_task_penalty(pool, task_id, due_date)` and
`test_utils::set_vacation_dates(pool, household_id, start, end)`. Rows only exist when the
background job actually ran, so tests MUST insert them explicitly.

**Reuse, do not reimplement:**
- `tasks_service::get_task(pool: &SqlitePool, task_id: &Uuid) -> Result<Option<Task>, TaskError>`
  (`backend/src/services/tasks.rs:109`)
- `household_settings::is_household_on_vacation(settings: &HouseholdSettings, today: NaiveDate) -> bool`
  (`backend/src/services/household_settings.rs:278`)
- `shared::HabitType::is_inverted(&self) -> bool` (`shared/src/types.rs:542`)
- `shared::RecurrenceType::OneTime`, `shared::SuggestionStatus::{Suggested, Approved, Denied}`

**Column values in the tasks table:** `habit_type` is `'good' | 'bad'`; `suggestion` is
`NULL | 'suggested' | 'approved' | 'denied'`; `recurrence_type` is
`'daily' | 'weekly' | 'monthly' | 'weekdays' | 'custom' | 'onetime'`.
</interfaces>

<pitfalls>
- **`process_missed_tasks` does NOT filter by suggestion status.** Verified: `background_jobs.rs`
  lines 194-198 fetch with a bare `SELECT * FROM tasks` and the loop only checks `OneTime`,
  `paused`, `archived`. Its siblings `process_auto_archive` (line 401) and
  `process_period_finalization` (line 536) DO apply `(suggestion IS NULL OR suggestion = 'approved')`.
  So `missed_task_penalties` is NOT guaranteed to be suggestion-clean. Apply the filter yourself —
  D-14 asks for it and it costs nothing.
- **`missed_task_penalties` is also not guaranteed archived-clean over time** — a task archived
  after a penalty was written still has its old rows. Apply the `archived`/`paused` filters
  yourself too (D-14 says these are excluded "everywhere").
- **Do not recompute "was this missed"** with `is_task_due_on_date` + `is_task_overdue` for
  yesterday. D-08 explicitly rejects live recomputation — the report shows what the system actually
  penalized.
- **Do not return bare `String` titles.** Plan 02's `ReportLine` and its single formatter are the
  contract (D-21, DRY).
</pitfalls>
</context>

<threat_model>
**Enforcement level:** ASVS Level 1. **Blocking severity:** `high`.

| ID | Threat | ASVS L1 ref | Severity | Mitigation in this plan | Verified by |
|---|---|---|---|---|---|
| T-03-A | **Cross-user data leakage via the penalty query.** `missed_task_penalties` has no `user_id` (D-15). A query that selects rows by `due_date` alone and prints their task titles exposes every household member's misses to any single member | V4.2.1 (data access scoped to the authorized subject) | **high** | Every penalty row is resolved to its `Task` and the D-13 rule is applied — `assigned_user_id == user_id` OR `assigned_user_id IS NULL`. The D-10 query applies the same rule in SQL as `(t.assigned_user_id = ? OR t.assigned_user_id IS NULL)`. | Test `test_missed_yesterday_excludes_other_users_penalty` and `test_missed_yesterday_excludes_other_users_indulged_bad_habit` |
| T-03-B | **Cross-household data leakage.** `missed_task_penalties` rows are keyed only by `task_id` + `due_date` — nothing in the table scopes them to a household. A `due_date`-only query returns other households' tasks | V4.2.1 | **high** | Every penalty row's resolved `Task` is checked with `task.household_id == *household_id` before it can contribute a line; the D-10 SQL joins `tasks` and binds `t.household_id = ?`. | Test `test_missed_yesterday_excludes_other_households` seeds a penalized task in a second household and asserts its title is absent |
| T-03-C | **Stale-state disclosure.** A task archived or paused after a penalty was recorded still has penalty rows; surfacing it leaks a title the user is no longer meant to see in active views | V4.2.1 | medium | `archived`, `paused` and non-approved `suggestion` are filtered on the resolved task, not assumed from the source table (D-14). | Tests `test_missed_yesterday_excludes_archived_task`, `..._excludes_paused_task`, `..._excludes_pending_suggestion` |
| T-03-D | **Untrusted content in generated text.** Task titles are user-controlled and land verbatim in `report_text` | V5.3.3 | low | Same contract as plan 02: plain text, JSON-escaped in transit, rendered as a `<pre>` text node (never `inner_html`) in plan 05. | Inherited from plan 02's `test_report_line_preserves_special_characters` |

**Residual `high` risk after mitigation:** none. T-03-A and T-03-B are each closed by a dedicated
automated test.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Missed-yesterday — the penalized-tasks half (D-08/D-09/D-13/D-14/D-15)</name>
  <files>backend/src/services/report.rs</files>

  <read_first>
    - backend/src/services/report.rs (the file being modified — plan 02's `build_missed_yesterday_section` stub, `ReportLine`, `format_report_line`, `sort_report_lines`, `ReportError`)
    - backend/src/services/background_jobs.rs (source of truth for miss semantics — `process_missed_tasks` lines 184-368 in full: the skip order OneTime → paused → archived, the vacation guard, the `(task_id, due_date)` completion lookup, the `missed_task_penalties` INSERT, and the ABSENCE of a suggestion filter; also read `process_auto_archive` line ~401 to see the suggestion clause its sibling does apply)
    - backend/src/services/scheduler.rs (source of truth for due semantics — confirm you are NOT re-deriving misses here)
    - backend/src/services/tasks.rs (`get_task` line 109)
    - backend/migrations/20240125000000_missed_task_tracking.sql
    - backend/src/test_utils.rs (post plan-01 — `insert_missed_task_penalty`, the task builder, `create_test_household_with_name`, `create_test_user`, `create_test_membership`)
  </read_first>

  <behavior>
    - `test_missed_yesterday_lists_penalized_task`: a daily good-habit task assigned to the caller with a `missed_task_penalties` row for yesterday appears as `- Vacuuming` (D-08).
    - `test_missed_yesterday_includes_unassigned_task`: `assigned_user_id = None` appears (D-13).
    - `test_missed_yesterday_excludes_other_users_penalty`: a penalized task assigned to user B is absent from user A's report (D-13, D-15, T-03-A).
    - `test_missed_yesterday_excludes_other_households`: a penalized task in a second household is absent (T-03-B).
    - `test_missed_yesterday_excludes_inverted_habit_penalty`: a `HabitType::Bad` task with a penalty row is absent — bad habits get a reward, not a penalty (D-09).
    - `test_missed_yesterday_excludes_one_time_task`: `RecurrenceType::OneTime` with a penalty row is absent (D-14 — missed-only exclusion).
    - `test_missed_yesterday_excludes_archived_task` / `..._excludes_paused_task`: absent (D-14, T-03-C).
    - `test_missed_yesterday_excludes_pending_suggestion`: `suggestion = Some(SuggestionStatus::Suggested)` absent; `Some(SuggestionStatus::Approved)` present (D-14, and `process_missed_tasks` does not filter this).
    - `test_missed_yesterday_ignores_other_dates`: a penalty row for two days ago is absent (D-12).
    - `test_missed_yesterday_line_shows_due_time`: a penalized task with `due_time = Some("20:00")` renders `- Vacuuming (by 20:00)` (D-21, resolved ambiguity 3).
    - `test_missed_yesterday_empty_renders_empty_state`: no penalty rows renders `All tasks completed yesterday` (D-23).
  </behavior>

  <action>
Replace the `build_missed_yesterday_section` stub body in `backend/src/services/report.rs`.

Step 1 — read the materialized penalty state (D-08, do NOT recompute):
`sqlx::query_scalar::<_, String>("SELECT task_id FROM missed_task_penalties WHERE due_date = ?")`
bound with `yesterday`, `.fetch_all(pool)`. `missed_task_penalties` has no `household_id` and no
`user_id` (D-15), so the query is intentionally broad and every filter below is applied on the
resolved `Task`.

Step 2 — for each returned `task_id` string: `Uuid::parse_str`, skip on parse failure, then
`tasks_service::get_task(pool, &task_id).await?`; skip `None`. Then apply, each with a comment
naming its decision ID:
- `task.household_id == *household_id` else `continue` — **T-03-B**, the household scope the source
  table cannot provide.
- `if task.archived || task.paused { continue; }` — D-14, T-03-C.
- suggestion filter — D-14: `continue` unless `task.suggestion.is_none() || task.suggestion == Some(SuggestionStatus::Approved)`.
  Required because `process_missed_tasks` does NOT apply it.
- `if task.recurrence_type == RecurrenceType::OneTime { continue; }` — D-14 missed-only exclusion.
- `if task.habit_type.is_inverted() { continue; }` — D-09.
- D-13 ownership: `task.assigned_user_id.map(|id| id == *user_id).unwrap_or(true)`; `continue` when
  false — **T-03-A**.

Step 3 — push `ReportLine { title: task.title, due_time: task.due_time, done: false }`. `done` is
always `false` in this section: a "done" marker on something you missed would be nonsense, and the
shared formatter therefore emits nothing extra.

Leave the D-10 half and the vacation guard to Task 2 of this plan; end this task by calling
`sort_report_lines(&mut lines)` and returning them.

Add the `<behavior>` tests to the existing `#[cfg(test)] mod tests`, seeding penalty rows with
`test_utils::insert_missed_task_penalty` (they never appear on their own — the background job must
have run).
  </action>

  <verify>
    <automated>nix develop -c cargo test -p backend services::report::</automated>
    <automated>nix develop -c cargo clippy -p backend --all-targets</automated>
  </verify>

  <acceptance_criteria>
    - `backend/src/services/report.rs` contains `SELECT task_id FROM missed_task_penalties WHERE due_date = ?`
    - `backend/src/services/report.rs` contains `RecurrenceType::OneTime` and `is_inverted()`
    - `backend/src/services/report.rs` contains a `household_id` equality check applied to the resolved task inside `build_missed_yesterday_section`
    - `backend/src/services/report.rs` does NOT contain `is_task_overdue` (no live miss recomputation — D-08)
    - `backend/src/services/report.rs` does NOT contain `ALTER TABLE` or `user_id` as a `missed_task_penalties` column reference (D-15 — no schema change)
    - `nix develop -c cargo test -p backend services::report::` exits 0 with at least 31 passing tests
    - `test_missed_yesterday_excludes_other_users_penalty` passes
    - `test_missed_yesterday_excludes_other_households` passes
    - `test_missed_yesterday_excludes_inverted_habit_penalty` passes
    - `nix develop -c cargo clippy -p backend --all-targets` exits 0
  </acceptance_criteria>

  <done>The penalized half of "Missed yesterday" reads materialized state, is correctly scoped to the caller and the household, and applies every D-14 exclusion — including the suggestion filter the background job omits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Missed-yesterday — indulged bad habits and the vacation guard (D-10/D-11/D-14)</name>
  <files>backend/src/services/report.rs</files>

  <read_first>
    - backend/src/services/report.rs (the file being modified — Task 1's `build_missed_yesterday_section`)
    - backend/src/services/background_jobs.rs (source of truth for miss semantics — `process_missed_tasks` lines 184-368; note it calls `is_household_on_vacation(&settings, today_local)` at processing time, while this report checks `yesterday`)
    - backend/src/services/scheduler.rs (source of truth for due semantics — `get_next_due_date` Weekdays branch lines 187-211 and Custom branch lines 213-226, which explain the KNOWN LIMITATION comment this task must add)
    - backend/src/services/household_settings.rs (`is_household_on_vacation` line 278 — read all four `(start, end)` Option combinations)
    - backend/src/services/tasks.rs (`complete_task` around lines 650-710 — confirms `complete_task` never checks vacation mode, which is why the guard is needed here)
    - backend/src/test_utils.rs (post plan-01 — `set_vacation_mode`, `set_vacation_dates`)
  </read_first>

  <behavior>
    - `test_missed_yesterday_lists_indulged_bad_habit`: a `HabitType::Bad` daily task assigned to the caller with a `task_completions` row for yesterday appears in the section (D-10).
    - `test_missed_yesterday_bad_habit_and_penalty_in_one_section`: a penalized good habit AND an indulged bad habit both appear under the single `Missed yesterday:` header with no sub-labels (D-11).
    - `test_missed_yesterday_excludes_other_users_indulged_bad_habit`: a bad habit assigned to user B is absent from user A's report (D-13, T-03-A).
    - `test_missed_yesterday_excludes_good_habit_completion`: a GOOD habit completed yesterday does not appear via the D-10 path (only inverted habits do).
    - `test_missed_yesterday_excludes_indulged_bad_habit_when_archived_paused_or_pending`: each exclusion checked (D-14).
    - `test_missed_yesterday_excludes_indulged_bad_habit_one_time`: `RecurrenceType::OneTime` bad habit absent (D-14).
    - `test_missed_yesterday_excludes_other_household_bad_habit`: a bad habit in a second household is absent (T-03-B).
    - `test_missed_yesterday_empty_during_vacation_with_penalty_rows`: vacation mode on with `vacation_start`/`vacation_end` spanning yesterday, plus BOTH a penalty row and an indulged bad habit — the section renders `All tasks completed yesterday` (resolved ambiguity 1: the guard covers the whole section).
    - `test_missed_yesterday_not_suppressed_when_vacation_range_excludes_yesterday`: vacation mode on but the range covers only today — yesterday's items still appear.
    - `test_missed_yesterday_sorted_by_due_time_then_title`: merged penalty + bad-habit lines sort by `due_time` (`None` last) then case-insensitive title (resolved ambiguity 2).
  </behavior>

  <action>
Extend `build_missed_yesterday_section` in `backend/src/services/report.rs`.

Step 1 — vacation guard covering the WHOLE section (resolved ambiguity 1, D-14). Add, at the very
top of the function before the penalty query:
`if household_settings::is_household_on_vacation(settings, yesterday) { return Ok(Vec::new()); }`
with a `//` comment recording that (a) `process_missed_tasks` naturally leaves the penalty half
vacation-clean but `complete_task` never checks vacation mode, so the D-10 half needs it, and
(b) this checks `yesterday` while the background job checks `today_local`, deliberately, because
the report describes yesterday.

Step 2 — the D-10 query, after the penalty loop. One `sqlx::query_as::<_, (String, Option<String>)>`
selecting `t.title, t.due_time` from `task_completions tc JOIN tasks t ON tc.task_id = t.id`, with
WHERE clauses, each commented with its decision ID:
- `tc.due_date = ?` bound to `yesterday` (D-10, literally as worded — add the
  `// KNOWN LIMITATION:` comment about Weekdays/Custom `due_date` skew here)
- `t.household_id = ?` bound to `household_id.to_string()` (T-03-B)
- `t.habit_type = 'bad'` (D-10 — only inverted habits; a good habit completed yesterday is a
  success, not a miss)
- `t.archived = 0 AND t.paused = 0` (D-14)
- `(t.suggestion IS NULL OR t.suggestion = 'approved')` (D-14)
- `t.recurrence_type != 'onetime'` (D-14)
- `(t.assigned_user_id = ? OR t.assigned_user_id IS NULL)` bound to `user_id.to_string()` (D-13, T-03-A)

Map each row to `ReportLine { title, due_time, done: false }` and extend the same `Vec` the penalty
loop built — one list, one section, no sub-labels (D-11).

Step 3 — de-duplicate before sorting: a task could theoretically contribute from both sources.
Drop exact-duplicate `(title, due_time)` pairs, preserving first occurrence, so the section never
prints the same line twice.

Step 4 — `sort_report_lines(&mut lines)` on the merged list, then return.

Do NOT introduce a second formatter — the caller (`format_report`, plan 02) already runs every line
through `format_report_line`, which is what gives this section D-21's `(by HH:MM)` suffix.

Add the `<behavior>` tests to the existing `#[cfg(test)] mod tests`, using
`test_utils::set_vacation_mode` + `test_utils::set_vacation_dates` for the vacation cases and
direct `sqlx::query` inserts into `task_completions` for the indulged-bad-habit cases.
  </action>

  <verify>
    <automated>nix develop -c cargo test -p backend services::report::</automated>
    <automated>nix develop -c cargo test --workspace</automated>
    <automated>nix develop -c cargo clippy -p backend --all-targets</automated>
  </verify>

  <acceptance_criteria>
    - `backend/src/services/report.rs` contains `is_household_on_vacation(settings, yesterday)`
    - `backend/src/services/report.rs` contains `t.habit_type = 'bad'`
    - `backend/src/services/report.rs` contains `(t.assigned_user_id = ? OR t.assigned_user_id IS NULL)`
    - `backend/src/services/report.rs` contains `t.recurrence_type != 'onetime'`
    - `backend/src/services/report.rs` contains the literal comment prefix `// KNOWN LIMITATION:`
    - `backend/src/services/report.rs` contains exactly one function named `format_report_line` (no second formatter)
    - `nix develop -c cargo test -p backend services::report::` exits 0 with at least 41 passing tests
    - `test_missed_yesterday_empty_during_vacation_with_penalty_rows` passes
    - `test_missed_yesterday_bad_habit_and_penalty_in_one_section` passes and asserts the output contains exactly one occurrence of the substring `Missed yesterday:`
    - `nix develop -c cargo test --workspace` exits 0
    - `nix develop -c cargo clippy -p backend --all-targets` exits 0
  </acceptance_criteria>

  <done>"Missed yesterday" merges penalized good habits and indulged bad habits into one unlabelled, deduplicated, sorted section, suppressed entirely on household vacation days, scoped to the caller and the household.</done>
</task>

</tasks>

<verification>
1. `nix develop -c cargo test -p backend services::report::` — all green, ≥41 tests.
2. `nix develop -c cargo test --workspace` — zero failures.
3. `nix develop -c cargo clippy -p backend --all-targets` — exits 0.
4. `git diff --stat` / `jj diff --stat` shows `backend/src/services/report.rs` as the ONLY changed
   file. No migration was added (D-15). `backend/src/services/scheduler.rs`,
   `backend/src/services/background_jobs.rs` and `backend/src/services/tasks.rs` are untouched (D-05).
</verification>

<success_criteria>
- `build_missed_yesterday_section` reads `missed_task_penalties` (D-08) and never recomputes misses.
- Inverted habits are excluded from the penalty half (D-09) and included via the completions half
  (D-10), in one section (D-11).
- The per-user filter is enforced by resolving each penalty row to its `Task` and applying D-13 —
  no `user_id` column, no migration (D-15).
- Archived / paused / pending-suggestion / one-time / vacation exclusions all hold (D-14).
- Lines use plan 02's single shared formatter with the D-21 due-time suffix.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

**No new files.** Modifies `backend/src/services/report.rs` only.

**Symbols completed (signatures already fixed by plan 02, bodies implemented here):**
- `build_missed_yesterday_section(pool: &SqlitePool, household_id: &Uuid, user_id: &Uuid, settings: &HouseholdSettings, yesterday: NaiveDate) -> Result<Vec<ReportLine>, ReportError>`

**New tests (≥21) in `backend::services::report::tests`**, including the two `high`-severity
security assertions `test_missed_yesterday_excludes_other_users_penalty` and
`test_missed_yesterday_excludes_other_households`.

**No migration, no schema change, no new shared type, no endpoint, no i18n key, no Cargo feature flag.**
</artifacts_produced>

<output>
After completion, create
`.planning/phases/02.1-daily-task-report-inserted-urgent/02.1-03-SUMMARY.md`.
Commit with `jj commit -m "feat(02.1-03): add missed-yesterday report section"`.
</output>
